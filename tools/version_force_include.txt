# Файлы, которые ДОЛЖНЫ попасть в version.json, даже если git их не видит
# (обычно потому что это собранный бинарник и лежит в .gitignore).
#
# Один путь на строку, относительно корня проекта. Если файла нет на диске —
# генератор просто пропустит его с предупреждением, сборка не упадёт.

XTTS Studio.exe
