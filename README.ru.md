<div align="center">

# 🎙️ XTTS Studio

**Портативное офлайн-приложение для клонирования голоса и синтеза речи на базе XTTS v2**

[![Windows](https://img.shields.io/badge/Windows-10%2F11%20x64-0078D6?logo=windows&logoColor=white)](https://img.shields.io/badge/Windows-10%2F11%20x64-0078D6?logo=windows&logoColor=white)
[![Offline](https://img.shields.io/badge/100%25-Offline-2da44e)](https://img.shields.io/badge/100%25-Offline-2da44e)
[![Portable](https://img.shields.io/badge/Portable-no%20install-orange)](https://img.shields.io/badge/Portable-no%20install-orange)
[![RU/EN](https://img.shields.io/badge/UI-RU%20%2F%20EN-58a6ff)](https://img.shields.io/badge/UI-RU%20%2F%20EN-58a6ff)
[![Themes](https://img.shields.io/badge/Themes-Dark%20%2F%20Light-7c3aed)](https://img.shields.io/badge/Themes-Dark%20%2F%20Light-7c3aed)

</div>

---

## 🚀 О проекте

**XTTS Studio** — полностью офлайн инструмент для синтеза речи и клонирования голоса.

Работает в портативном режиме, не требует установки и не использует интернет.

AI-модуль опционален — подключается через любой OpenAI-совместимый провайдер.

---

## 📥 Скачать

> ⚠️ Google Drive может показать предупреждение о большом размере файла перед скачиванием — это нормально: файл не проверяется антивирусом Google из-за размера, а не из-за угрозы.

| Версия | Размер | Ссылка |
|---|---|---|
| ⚙️ CPU (default) + автоустановка CUDA  | 5 ГБ | [📥 Download XTTS Studio (Google Drive)](https://drive.google.com/file/d/1RJfaMjVHV_NUaaHgg4uSd0B8DI9noxRs/view?usp=drive_link) |

📜 **Лицензия:** [LICENSE.md](./LICENSE.md) — свободное использование с обязательным указанием автора

---

## ✨ Возможности

### 🎤 Синтез и клонирование
- Полностью офлайн — никаких внешних запросов
- Portable — одна папка, любой Windows ПК
- Клонирование голоса по референсу 10–20 секунд
- Библиотека голосов с кэшем Speaker Embedding
- Поддержка длинных текстов без ограничений
- Авто-переключение языка RU/EN внутри одного текста
- **CUDA по запросу** — по умолчанию работает на CPU; включение CUDA в Настройках ставит библиотеки строго под обнаруженную видеокарту NVIDIA. Без поддерживаемой NVIDIA-карты — остаётся оптимизированный CPU-режим.

### 🖥 Интерфейс
- 🌗 **Окно «Настройки»** — Единая системная панель настроек (доступна по кнопке **⚙ Настройки** на тулбаре), разделенная на 3 секции:
  1. **Обновления** — управление автопроверкой обновлений на старте и ручной запуск обновления.
  2. **Ускорение (CPU/GPU)** — отображение железа, версии PyTorch, выбор предпочитаемого режима с сохранением, и установка CUDA/CPU пакетов с отображением прогресса.
  3. **Диагностика** — сканирование мусора всего проекта, комплексная диагностика 10 библиотек и инструмент самовосстановления.
- 🌗 **Темы оформления:** тёмная и мягкая светлая («молочная») из коробки, а также встроенный **конструктор тем** с полной цветовой кастомизацией, поддерживающий **Immersive Dark/Light Mode** системного заголовка на Windows для всех окон (включая Настройки).
- 🧩 **Настраиваемое рабочее пространство:** боковую панель можно разместить слева или справа, сворачивать и автоматически сохранять её положение
- 🪟 **Гибкая компоновка интерфейса:** возможность изменять расположение и отображение панелей под собственный стиль работы
- 📐 Адаптивный интерфейс, автоматически подстраивающийся под размер окна
- 🌐 Полная локализация интерфейса (RU / EN), включая AI-чат и настройки провайдеров
- 🔠 Регулируемый размер текста в редакторе (ползунок «Aa»)
- Все настройки вида, включая расположение панелей и неоновую подсветку, сохраняются между сессиями
- 💡 **Неоновая подсветка кнопок** — включается и выключается по желанию, цвет свечения настраивается под свою тему
- 📐 Адаптивный тулбар — панели растягиваются под размер окна
- ◀ Выдвижная боковая панель с запоминанием положения
- 🔄 **Автообновление** — поэтапная (staged) загрузка новой версии, проверка целостности через **SHA256**, автоматический бэкап и откат (rollback) при сбое, определение необходимости полной переустановки по `min_app_version`

### 🧠 Обработка текста
- **Автоматическая Ёфикация** — контекстное восстановление буквы «Ё» во всех однозначных русских словах (`ещё`, `моё`, `своё` и др.), существенно повышающее качество ударений и интонаций голоса.
- **Защита инициалов от деления** — предложения в Чанкере делятся с использованием Negative Lookahead/Lookbehind, предотвращая разрез фраз на инициалах (например, `А. С. Пушкин` теперь читается слитно и естественно).
- **Чистые Prosody-паузы** — устранение артефактов «двойного вдоха» на стыках интонационных многоточий и пауз.
- Числа → слова автоматически
- Аббревиатуры с точками и слэшами → фонетический словарь (авто + ручной), включая развертывание физических и математических выражений (`км/ч` → `километров в час`, `т.д.` → `так далее`)
- Нормализация текста перед генерацией

### 🎛 Управление качеством
- 4 пресета: ⭐ Высокое качество / 📖 Нарратив / ⚡ Динамика / 🎭 Экспрессия
- Тонкая настройка: temperature, top_p, repetition_penalty, скорость, trim
- Контроль качества чанков — авто-перегенерация при повторах и обрывах
- Де-эссер, RMS-нормализация громкости, авто-обрезка тишины
- Кэш чанков — повторная генерация того же текста не тратит время

### 🤖 AI-модуль (опционально)
- **AI Conductor** — анализирует текст и назначает параметры XTTS для каждого чанка индивидуально
  - Уровень 1: temperature, speed, паузы по контексту и интонации
  - Уровень 2: rewrite текста под заданный жанр или настроение (с negative prompt)
  - Оба уровня полностью независимы — включение/выключение одного не влияет на другой (проверка задублирована и в логике кондуктора, и в раннере)
- **AI чат** — встроенный чат-ассистент с историей сессий и поиском
  - Режим редактора текста и режим свободного чата
  - Кнопка «Улучшить» — технический rewrite текста для лучшего TTS
  - Раскрывающиеся карточки настроек провайдера (аккордеон) вместо длинной формы
- **Окно «AI статус»** — цепочка провайдеров и статус каждого в реальном времени
- Поддержка цепочки провайдеров: Groq, OpenRouter, локальные LLM (офлайн, без ключей), кастомные OpenAI-совместимые
- Каталог провайдеров, библиотека ключей; выбранный пользователем провайдер всегда идёт первым в цепочке
- **Локальные LLM (офлайн, без ключей и интернета)**
  - Автоматическое сканирование окружения ПК (наличие GPU, CUDA, объём VRAM/RAM) для подбора совместимой конфигурации
  - Автоматическая установка необходимых библиотек под выбранную модель — без ручной возни с pip
  - Встроенный каталог поддерживаемых локальных моделей с загрузкой прямо из интерфейса
  - Возможность подключить собственную модель — достаточно указать папку с её файлами

### 📋 Прочее
- Очередь задач с отменой
- Пакетная обработка TXT-файлов
- История генераций с возвратом текста
- Подсветка текущего чанка в реальном времени
- Статистика: время, чанки, голос, скорость
- Автосохранение настроек между сессиями
- Экспорт в WAV и MP3

---

## 🖼 Скриншоты

<p align="center">
  <img src="images/main.PNG" width="45%" />
  <img src="images/ai-assist.PNG" width="45%" />
</p>

<p align="center">
  <img src="images/mail-light.PNG" width="45%" />
  <img src="images/ai-settings.PNG" width="45%" />
  <img src="images/settings.PNG" width="45%" />
</p>

---

## 🚀 Быстрый старт

1. Скачайте и распакуйте архив
2. Не используйте путь с кириллицей
3. Запустите `XTTS Studio.exe`
4. Выберите или загрузите голосовой референс
5. Введите текст
6. Нажмите **🚀 ГЕНЕРИРОВАТЬ**
7. Результат сохраняется в `outputs/` — кнопка **🎵 Аудио**

---

## ⚙️ Как работает

```
Референс → авто-обработка → библиотека голосов
   ↓
Текст → нормализация → числа в слова → аббревиатуры → Ёфикация
   ↓
(опционально) AI-улучшение / rewrite текста
   ↓
Разбивка на чанки → защита инициалов → простановка пауз
   ↓
(опционально) AI Conductor — параметры для каждого чанка
   ↓
Генерация с контролем качества и кэшированием
   ↓
Сборка → нормализация громкости → де-эссер → WAV / MP3
```

---

## 🛠 Диагностика и Самовосстановление (Error Recovery)

Приложение оснащено интеллектуальной системой мониторинга здоровья и самолечения среды, доступной в разделе **«Диагностика»** панели настроек:

- **Самопроверка при запуске** — при каждом старте проверяется torch; если он сломан или отсутствует, он подтягивается из кэша (если есть) или переустанавливается заново совместимой версией — без участия пользователя.
- **Диагностика системного окружения** — запускает изолированный проверочный процесс, тестирующий импорт и работу **10 ключевых библиотек** (`numpy`, `torch`, `torchaudio`, `torchvision`, `TTS`, `soundfile`, `pygame`, `customtkinter`, `num2words`, `llama_cpp`).
- **Сканирование мусора** — рекурсивная глубокая очистка всей директории `C:\XTTS Studio` от кэша (`__pycache__`, `.pytest_cache`), логов (`logs/`), временных файлов (`.tmp`, `.bak`).
- **Транзакционный карантин** — файлы перед удалением переносятся во внешний буфер `Quarantine/`. Система сравнивает baseline-ошибки до и после. Если обнаружен новый сбой — все файлы автоматически возвращаются обратно, а виновник заносится в черный список.
- **Защита системных файлов** — пустые служебные файлы (вроде `__init__.py` или `py.typed`) и манифесты (`requirements.txt`, `checksums.txt`, настройки и др.) строго защищены от удаления.
- **Устранение ошибок (Error Recovery)** — утилита самолечения, которая по реестру удалений точечно вычисляет поврежденные библиотеки и переустанавливает их начисто через pip в **безопасном режиме перезапуска** для снятия DLL-блокировок Windows.

---

## 🧠 Словарь произношений

Примеры встроенных правил:
```
AI      → эй ай
CPU     → си-пи-ю
GPU     → джи-пи-ю
OpenAI  → ОпенЭйАй
```
Словарь пополняется автоматически при генерации, а если включён **AI Conductor** — он дополнительно помогает словарю: анализирует транслит и сам добавляет исправления в категорию `ai_corrected`, то есть активный кондуктор фактически «доучивает» словарь параллельно с озвучкой.

Редактирование — кнопка **📖 Словарь** (добавление, изменение, удаление правил).
- **Категории правил с приоритетом:** `builtin → auto → ai_corrected → custom` — более специфичное правило перекрывает более общее
- **Diagnostic dry-run режим** — предпросмотр того, что изменится, без записи в файл
- **Автоматические таймстамп-бэкапы** в `word_rules_backups/` перед каждым сохранением
- Метаданные у каждой записи (категория, источник)

---

## 💻 Требования

| | По умолчанию (CPU) | С включённой CUDA |
|---|---|---|
| ОС | Windows 10/11 x64 | Windows 10/11 x64 |
| Память | 8+ ГБ RAM | 8+ ГБ RAM |
| GPU | — | NVIDIA, 4+ ГБ VRAM, Compute Capability 6.0+ |
| Скорость | медленнее реального времени | быстрее реального времени |

> ℹ️ Сразу после распаковки работает на CPU, никакой настройки не нужно. CUDA — опционально: включается в **⚙ Настройки → Ускорение**, определяет видеокарту NVIDIA и ставит только нужные библиотеки. Нет поддерживаемой карты — остаётся CPU.

---

## ⚠️ Важно

Не используйте пути с кириллицей:
```
✔ C:\XTTS\
✘ C:\Новая папка\XTTS\
```

---

# 🗂 Полная структура проекта

**Точка входа:** `XTTS Studio.exe` → BAT → `python\runtime\python.exe` → `gui.py` → зависимости из `python\xtts_env`

Проект имеет модульную архитектуру: тонкая точка входа, техническое ядро `engine/` (без GUI-зависимостей) и слой интерфейса `engine/gui/`.

## Дерево проекта

```
XTTS Studio (portable)
│
├── gui.py                        ← точка входа: только запуск интерфейса
├── i18n.py                       ← локализация интерфейса (RU / EN)
├── settings.json                 ← настройки сессии: пресеты, тема, язык, панель (авто)
├── gpt_settings.json             ← провайдер AI, ключи, модели (авто)
├── word_rules.json               ← словарь произношений (builtin/auto/ai_corrected/custom)
├── chat_history.json             ← история AI чат-сессий
├── history.json                  ← история генераций
├── version.json                  ← версия и данные для системы обновлений (min_app_version)
├── env_cache.cfg                  ← кэш результатов сканирования окружения (GPU/CUDA/библиотеки)
├── generate_version_manifest.py  ← генерация манифеста версии для системы обновлений
├── theme_settings.json           ← сохранённая пользовательская тема/цветовая схема (из конструктора тем)
├── checksums.txt                 ← контрольные суммы файлов для проверки обновлений (SHA256)
├── .llama_broken_backends.json   ← список нерабочих бэкендов llama.cpp на этом ПК (авто-фолбэк при следующем запуске)
│
├── engine/                       ═══ ТЕХНИЧЕСКОЕ ЯДРО (без tkinter) ═══
│   │
│   │   ── пайплайн генерации ──
│   ├── tts_runner.py             ← тонкая точка входа (сама логика перенесена в engine/tts/)
│   ├── chunker.py                ← разбивка текста на чанки, защита инициалов (SBD)
│   ├── normalizer.py             ← числа→слова, аббревиатуры, пунктуация, авто-Ёфикация
│   ├── word_replacer.py          ← фонетические замены по словарю, категории правил
│   ├── word_replacer_window.py   ← бэкенд окна словаря (dry-run, бэкапы)
│   ├── text_utils.py             ← общие текстовые хелперы
│   ├── smart_pauses.py           ← паузы между чанками (без кондуктора)
│   ├── prosody_layer.py          ← смысловая просодия, очистка двойных пауз
│   ├── de_esser.py               ← подавление шипящих
│   │
│   │   ── AI-модуль ──
│   ├── ai_conductor.py           ← AI Conductor (параметры чанков + rewrite)
│   ├── chat_window.py            ← бизнес-логика чата: провайдеры, история сессий
│   ├── gpt_client.py             ← AI-клиент: облачные провайдеры, ключи, fallback-цепочка
│   ├── local_llm_client.py       ← клиент для локальных (офлайн) LLM
│   ├── local_env_section.py      ← настройка локального AI-окружения
│   ├── env_setup.py              ← установка/проверка окружения для AI-модуля, глубокий скан
│   │
│   │   ── голос и аудио ──
│   ├── reference_processor.py    ← конвертация и нормализация референса
│   ├── voice_manager.py          ← библиотека голосов
│   ├── audio_backend.py          ← инициализация аудио (pygame)
│   │
│   │   ── инфраструктура ──
│   ├── task_manager.py           ← многопоточная очередь задач с отменой
│   ├── task_models.py            ← модель задачи Task
│   ├── batch_window.py           ← бизнес-логика пакетной обработки TXT
│   ├── updater.py                ← система обновлений: staged-загрузка, проверка SHA256, бэкап/rollback, самоперезапуск
│   ├── paths.py                  ← базовые директории проекта
│   ├── settings_store.py         ← чтение settings.json
│   ├── history_store.py          ← хранилище истории генераций
│   ├── output_naming.py          ← именование выходных файлов
│   ├── text_tools.py             ← нормализация текста для GUI
│   └── logging_utils.py          ← файловое логирование
│
│   ├── tts/                      ═══ вспомогательные TTS-утилиты ═══
│   │   ├── __init__.py           ← ядро генерации (normalize → chunk → generate → merge)
│   │   ├── cache.py              ← кэш готовых чанков (md5)
│   │   ├── device.py             ← автодетект CUDA/CPU
│   │   ├── export.py             ← экспорт в WAV/MP3
│   │   ├── qc.py                 ← контроль качества: детектор зацикливания, валидатор длительности
│   │   └── utils.py              ← общие TTS-хелперы
│   │
│   └── gui/                      ═══ ИНТЕРФЕЙС (tkinter / customtkinter) ═══
│       │
│       │   ── ядро окна ──
│       ├── main_window.py        ← сборка главного окна (оркестратор)
│       ├── layout.py             ← раскладка + выдвижная левая панель
│       ├── theme.py              ← темы (тёмная / светлая) + конструктор своих тем, titlebar
│       ├── theme_manager.py      ← применение и переключение темы
│       ├── colors.py             ← палитры обеих тем
│       ├── widgets.py            ← фабрики виджетов, CTk-совместимость
│       ├── tooltip.py            ← всплывающие подсказки
│       ├── gradient.py           ← градиентный фон
│       ├── neon_widgets.py       ← неоновая подсветка кнопок: вкл/выкл, настраиваемый цвет свечения
│       ├── helpers.py            ← общие GUI-хелперы
│       │
│       │   ── панели главного окна ──
│       ├── header_panel.py       ← шапка: Обновить / AI статус / RU-EN
│       ├── voice_panel.py        ← референс + библиотека голосов
│       ├── player.py             ← плеер референса
│       ├── queue_panel.py        ← очередь задач
│       ├── batch_panel.py        ← панель запуска пакетной обработки
│       ├── chat_panel.py         ← панель запуска AI-чата
│       ├── word_replacer_panel.py← панель запуска окна словаря
│       ├── console.py            ← встроенная консоль
│       ├── textbox.py            ← окно ввода: DnD, подсветка чанков, размер текста
│       ├── toolbar.py            ← адаптивный тулбар: Файл / AI / Вывод / Действие
│       ├── statusbar.py          ← прогресс-бар и статус
│       │
│       │   ── логика GUI ──
│       ├── generation.py         ← запуск/отмена генерации, callbacks
│       ├── presets.py            ← пресеты качества + окно настроек
│       ├── settings_ui.py        ← сохранение и применение настроек
│       ├── styles_menu.py        ← меню «Стили»
│       ├── updates.py            ← сборка окна Настройки, Диагностика и очистка
│       │
│       │   ── отдельные окна ──
│       ├── chat_window.py        ← главный интерфейс окна AI-чата, использует модули и ядро из chat_window/
│       ├── ai_conductor.py       ← окно AI Conductor
│       ├── ai_status_window.py   ← окно «AI статус»: цепочка провайдеров
│       ├── history_window.py     ← окно «История»
│       ├── output_window.py      ← окно «Аудио» со встроенным плеером
│       ├── batch_window.py       ← окно пакетной обработки TXT
│       ├── word_replacer_window.py ← окно словаря произношений (dry-run, бэкапы)
│       ├── dialogs.py            ← язык озвучки, справка
│       │
│       └── chat_window/          ═══ модульное окно AI-чата (пакет) ═══
│           ├── chat_window.py    ← сборка окна чата
│           ├── auto_install_local_ai.py ← авто-установка окружения для локальных LLM прямо из окна чата
│           ├── chat_messages.py  ← рендер сообщений
│           ├── chat_input.py     ← поле ввода
│           ├── chat_actions.py   ← действия над сообщениями (копировать/удалить/озвучить…)
│           ├── chat_editor.py    ← режим редактора текста
│           ├── chat_export.py    ← экспорт переписки
│           ├── chat_history.py   ← список и загрузка сессий
│           ├── chat_scroll.py    ← поведение прокрутки
│           ├── chat_search.py    ← поиск по сообщениям
│           ├── chat_settings.py  ← карточки настроек провайдеров (аккордеон)
│           ├── chat_typing.py    ← индикатор набора текста
│           ├── custom_widgets.py ← кастомные виджеты чата
│           ├── hotkeys.py        ← горячие клавиши чата
│           ├── placeholders.py   ← плейсхолдеры пустых состояний
│           ├── state.py          ← общее состояние окна
│           ├── theme_manager.py  ← тема чата
│           ├── theme_settings.py ← настройки темы чата
│           ├── ui_utils.py       ← UI-хелперы чата
│           └── engine/           ← логика чата, отделённая от UI
│               ├── generation.py ← запуск генерации ответа ассистента
│               ├── sessions.py   ← управление сессиями чата
│               └── utils.py      ← хелперы
│
├── models/xtts_v2/               ← модель (офлайн)
├── library/[voice_name]/         ← голосовые профили + кэш embedding (CPU/CUDA)
├── outputs/_cache/                ← готовые файлы + кэш чанков (md5)
├── logs/                         ← логи ошибок
├── reference/                    ← исходные референс-файлы
├── word_rules_backups/           ← таймстамп-бэкапы словаря произношений
├── test/                         ← pytest: chunker, normalizer, smart_pauses, updater
├── tools/                        ← вспомогательные утилиты разработки
├── ffmpeg/bin/                   ← ffmpeg.exe, ffprobe.exe
└── python/
    ├── xtts_env/                 ← venv с зависимостями
    └── runtime/                  ← Python 3.11 portable
```

> ℹ️ `engine/gui/chat_window.py` — главный файл интерфейса окна чата (сборка UI, компоновка). Пакет `engine/gui/chat_window/` — это его внутренности: подмодули (сообщения, ввод, история, поиск, экспорт и т.д.) и вложенное `chat_window/engine/` с логикой генерации ответов и сессий, на которые `chat_window.py` опирается.

---

## 🔬 Модули engine/ — по зонам ответственности

### Пайплайн генерации
- **`tts_runner.py`** — тонкая точка входа; основная логика `run_tts()` перенесена в пакет **`engine/tts/`**: normalize → word replacer → chunk → conductor → generate → merge. Ленивая загрузка модели (thread-safe singleton), кэш embedding и готовых чанков (md5), авто-trim по тишине, RMS-нормализация громкости.
- **`engine/tts/qc.py`** — контроль качества: детектор зацикливания + валидатор длительности, авто-перегенерация.
- **`engine/tts/device.py`** — автодетект CUDA/CPU с фолбэком на CPU, если видеокарта не поддерживается.
- **`engine/tts/cache.py`** / **`export.py`** — кэш чанков и экспорт в WAV/MP3.
- **`chunker.py`** — разбивка на предложения, нарезка длинных, объединение коротких, предотвращение деления инициалов (SBD), проверка плохого начала/конца чанка.
- **`normalizer.py`** — числа→слова, аббревиатуры, пунктуация; автоматическая Ёфикация, отдельно ритм для латинских/кириллических аббревиатур и CamelCase.
- **`word_replacer.py`** — фонетические замены по словарю с приоритетом категорий `builtin → auto → ai_corrected → custom`), автотранслитерация терминов; **`word_replacer_window.py`** добавляет-диагностический dry-run режим и автоматические таймстамп-бэкапы в `word_rules_backups/` перед каждым сохранением.
- **`smart_pauses.py` / `prosody_layer.py`** — паузы и смысловая просодия; **оба пропускаются, если активен AI Conductor** — тогда паузы и temperature schedule берутся из `conductor_map`.

### AI-модуль
- **`ai_conductor.py`** — `conduct()`: один вызов на весь текст, анализирует чанки и возвращает параметры озвучки (temperature/top_p/repetition_penalty/speed/pause_after_ms) для каждого. Опционально — переработка текста под стиль `rewrite_enabled`) и проверка транслита `corrections` → `word_rules.json`). При ошибке ИИ — фолбэк на дефолтные параметры, генерация не прерывается. Уровни 1 и 2 явно разделены флагами и в этом модуле, и в `tts_runner.py`, чтобы они не могли случайно повлиять друг на друга.
- **`gui/chat_window.py` + `gui/chat_window/`** — окно AI-чата: `chat_window.py` — главный файл интерфейса (сборка UI), а пакет `chat_window/` содержит его подмодули: рендер сообщений, ввод, история и поиск сессий, экспорт переписки, режим редактора текста и режим свободного чата, аккордеон настроек провайдеров, горячие клавиши, индикатор набора текста. Логика генерации ответа и управление сессиями вынесены во вложенный `chat_window/engine/`. Полностью локализовано (RU/EN).
- **`gpt_client.py`** — цепочка облачных провайдеров (активный → встроенные → кастомные), управление ключами и моделями, каталог провайдеров.
- **`local_llm_client.py`** / **`local_env_section.py`** / **`env_setup.py`** — поддержка локальных (офлайн) LLM как альтернативы облачным провайдерам: автоматическое сканирование окружения ПК (GPU/CUDA/VRAM), автоматическая установка нужных библиотек под выбранную модель, встроенный каталог поддерживаемых моделей и возможность подключить свою модель из папки.
- **`gui/ai_status_window.py`** — диагностическое окно, показывающее цепочку провайдеров и статус каждого.

### Голос и аудио
- **`reference_processor.py`** — конвертация референса в WAV, SNR-проверка, кэш.
- **`voice_manager.py`** — сканирование `library/`, список голосов, активный голос.
- **`de_esser.py`** — подавление шипящих на финальном файле.

### Инфраструктура
- **`task_manager.py` / `task_models.py`** — многопоточная очередь генерации с отменой по id.
- **`updater.py`** — система обновлений: поэтапная (staged) загрузка новой версии, проверка целостности каждого файла через **SHA256**, автоматический бэкап текущей версии и rollback при сбое, определение необходимости полной переустановки через `min_app_version` (корректная обработка пробелов в именах файлов через URL-кодирование), самоперезапуск.
- **`i18n.py`** — словарь переводов RU/EN (350+ ключей), автозагрузка сохранённого языка.

---

## 🔐 Как устроено обновление

Система обновлений построена по принципу «безопасно, даже если что-то пойдёт не так»:

1. **Проверка версии** — текущая версия сверяется с `version.json`; поле `min_app_version` определяет, достаточно ли обычного патча или требуется полная переустановка.
2. **Поэтапная (staged) загрузка** — новая версия скачивается во временную папку, не затрагивая рабочие файлы приложения.
3. **Проверка SHA256** — каждый скачанный файл сверяется по контрольной сумме; при несовпадении хэша обновление прерывается и рабочая версия остаётся нетронутой.
4. **Бэкап и откат (rollback)** — перед заменой файлов создаётся резервная копия текущей версии; если применение обновления завершилось ошибкой, приложение автоматически откатывается на бэкап.
5. **Самоперезапуск** — после успешного применения обновления приложение перезапускается само.

Если автообновление всё же не сработало, `XTTS_DIAG.bat` предоставляет форсированный вариант восстановления/переустановки.

---

## 🗃 Данные и конфиги

| Файл | Назначение |
|---|---|
| `settings.json` | сессия: пресеты, флаги, тема, язык UI, положение панели, размер текста |
| `gpt_settings.json` | провайдер AI, ключи, модели |
| `word_rules.json` | словарь произношений `builtin` / `auto` / `ai_corrected` / `custom`) |
| `word_rules_backups/` | таймстамп-бэкапы словаря перед каждым сохранением |
| `chat_history.json` | история AI чат-сессий |
| `history.json` | история генераций |
| `version.json` | текущая версия и данные для системы обновлений (включая `min_app_version`) |
| `checksums.txt` | контрольные суммы файлов (SHA256) для проверки целостности при обновлении |
| `env_cache.cfg` | кэш результатов сканирования окружения (GPU/CUDA/библиотеки) |
| `theme_settings.json` | сохранённая пользовательская тема/цветовая схема из конструктора тем |
| `.known_safe_files.json` | реестр кэша безопасных файлов и история удалений (для диагностики и восстановления) |
| `.llama_broken_backends.json` | список нерабочих бэкендов llama.cpp на этом ПК, чтобы не пытаться их снова |

---

## 🧩 Разработка

Приложение разработано с использованием AI-инструментов: **Claude**, **ChatGPT** и других.

Рефакторинг архитектуры (разделение на `engine/` + `engine/gui/`), локализация RU/EN, светлая тема и доработки интерфейса выполнены с помощью **[Arena.ai](https://arena.ai) Agent Mode** (мульти-модельный агент: Claude, ChatGPT, Gemini и др.).

Ключевые модули `updater.py`, `chunker.py`, `normalizer.py`, `smart_pauses.py`) покрыты тестами **pytest** `test/`, запуск через `RUN_TESTS.bat`).

Вспомогательные инструменты разработки в `tools/`:
- `generate_version_files.py` — генерация `version.json` и манифеста обновления
- `convert_py_to_txt.bat` — конвертация `.py` в `.txt` для передачи в AI-чат
- `analyze.ps1` — снимок структуры проекта для AI-контекста между сессиями разработки
- `git_update.py` / `git_update.bat` — публикация версии на GitHub (сам апп при этом обновляется независимо, через `updater.py` и SHA256/staged/rollback, а не через Git)
- `cleanup_project.ps1` / `restore_quarantine.ps1` — очистка проекта от лишних файлов с карантином `_quarantine/`) вместо безвозвратного удаления, с возможностью отката

---

## ⚖️ Сторонние компоненты

Проект использует модель **XTTS v2** (Coqui), распространяемую под лицензией [Coqui Public Model License (CPML)](https://coqui.ai/cpml). Использование модели регулируется условиями CPML независимо от лицензии данного проекта.

---

## ☕ Поддержка проекта

**BTC:** `bc1qz78u3lvagt3v886359glv57ct6rnlh506wjmdy`

---

<div align="center">

**XTTS Studio** · by EXIZ10TION · Made with ❤️ 

</div>